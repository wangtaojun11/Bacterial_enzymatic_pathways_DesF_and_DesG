---
title: "cell_line_androgens"
author: "Taojun Wang"
date: "2024-01-07"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
getwd()
```
## load required packages
```{r warning=FALSE}
library(ggplot2)
library(ape)
library(plyr)
library(RColorBrewer)
library(reshape2)
library(scales)
library(dplyr)
library(ggpubr) 
library(cowplot) 
library(readr)
library(stats)
library(readr)
```

# Load the data
```{r}
d1 <- read_csv("C:/UIUC documents/Androgen manuscript/Data and codes/11OHT_LNCaP_overtime.csv") #read the data
head(d1)
```

```{r}
d1_24h <- subset(d1, Time == "24h")
d1_48h <- subset(d1, Time == "48h")
d1_96h <- subset(d1, Time == "96h")
```

# define format
```{r}
format.A <-  theme_classic()+ theme(plot.title = element_text(size= 11, hjust=0.5, face="bold"))+ 
  theme(axis.text = element_text(size = 10)) + theme(axis.title = element_text(size = 10, face="bold")) + 
  theme(strip.text = element_text( size = 10, face= "bold")) + theme(legend.position = "none")

format.B <-  theme_classic()+ theme(plot.title = element_text(size= 11, hjust=0.5, face="bold"))+ 
  theme(axis.text = element_text(size = 10)) + theme(axis.title = element_text(size = 10, face="bold")) + 
  theme(strip.text = element_text( size = 10, face= "bold")) 
```

# color
```{r}
display.brewer.pal(n = 8, name = 'Dark2')
# Hexadecimal color specification 
brewer.pal(n = 8, name = "Dark2")
```

# Clarify the factors
```{r}
d1_24h$Androgens = factor(d1_24h$Androgens,order = TRUE, levels = c("VC", "11OHT"))
d1_48h$Androgens = factor(d1_48h$Androgens,order = TRUE, levels = c("VC", "11OHT"))
d1_96h$Androgens = factor(d1_96h$Androgens,order = TRUE, levels = c("VC", "11OHT"))
```

# Summarize the d1_24h
```{r}
d1_24h_summary <- d1_24h %>%
  group_by(Androgens) %>%
  summarise(
    Mean = mean(Relative_cell_proliferation),
    SD = sd(Relative_cell_proliferation) # This avoids a grouped df warning in ggplot
  )

d1_24h_summary$Relative_cell_proliferation <- d1_24h_summary$Mean
head(d1_24h_summary)
##View(d1_24h_summary)

```

```{r}

p1 <- ggplot(d1_24h, aes(x = Androgens, y = Relative_cell_proliferation, fill = Androgens)) +
  geom_bar(data = d1_24h_summary, aes(y = Relative_cell_proliferation), stat = "identity", position = position_dodge(),color="black") +
  geom_point(position = position_jitterdodge(jitter.width = 0.2, dodge.width = 0.9), aes(color = Androgens), 
             shape = 21, size = 3, stroke = 0.5, fill = "white")  + 
  geom_errorbar(data = d1_24h_summary, aes(ymin = Relative_cell_proliferation - SD, ymax = Relative_cell_proliferation + SD), 
                width = .2, position = position_dodge(0.9)) + 
  scale_fill_manual(values = c( "#666666","#666666")) + 
  scale_color_manual(values = c( "black" ,"black")) + ylim(0,2) + 
  format.A + labs(x="Androgens (10 nM)", y="Relative cell proliferation (24 hours)")

print(p1)

```

```{r}
pairwise.t.test(d1_24h$Relative_cell_proliferation, d1_24h$Androgens, p.adjust.method = "BH")
```

# Summarize the d1_48h
```{r}
d1_48h_summary <- d1_48h %>%
  group_by(Androgens) %>%
  summarise(
    Mean = mean(Relative_cell_proliferation),
    SD = sd(Relative_cell_proliferation) # This avoids a grouped df warning in ggplot
  )

d1_48h_summary$Relative_cell_proliferation <- d1_48h_summary$Mean
head(d1_48h_summary)
##View(d1_48h_summary)

```

```{r}
p2 <- ggplot(d1_48h, aes(x = Androgens, y = Relative_cell_proliferation, fill = Androgens)) +
  geom_bar(data = d1_48h_summary, aes(y = Relative_cell_proliferation), stat = "identity", position = position_dodge(),color="black") +
  geom_point(position = position_jitterdodge(jitter.width = 0.2, dodge.width = 0.9), aes(color = Androgens), 
             shape = 21, size = 2.5, stroke = 0.5, fill = "white")  + 
  geom_errorbar(data = d1_48h_summary, aes(ymin = Relative_cell_proliferation - SD, ymax = Relative_cell_proliferation + SD), 
                width = .2, position = position_dodge(0.9)) + 
  scale_fill_manual(values = c("#666666","#666666")) + 
  scale_color_manual(values = c("black" ,"black" )) + ylim(0,2) +
  format.A + labs(x="Androgens (10 nM)", y="Relative cell proliferation (48 hours)")

print(p2)

```

```{r}
pairwise.t.test(d1_48h$Relative_cell_proliferation, d1_48h$Androgens, p.adjust.method = "BH")
```

# Summarize the d1_96h
```{r}
d1_96h_summary <- d1_96h %>%
  group_by(Androgens) %>%
  summarise(
    Mean = mean(Relative_cell_proliferation),
    SD = sd(Relative_cell_proliferation) # This avoids a grouped df warning in ggplot
  )

d1_96h_summary$Relative_cell_proliferation <- d1_96h_summary$Mean
head(d1_96h_summary)
## View(d1_96h_summary)

```

```{r}

p3 <- ggplot(d1_96h, aes(x = Androgens, y = Relative_cell_proliferation, fill = Androgens)) +
  geom_bar(data = d1_96h_summary, aes(y = Relative_cell_proliferation), stat = "identity", position = position_dodge(),color="black") +
  geom_point(position = position_jitterdodge(jitter.width = 0.2, dodge.width = 0.9), aes(color = Androgens), 
             shape = 21, size = 2.5, stroke = 0.5, fill = "white")  + 
  geom_errorbar(data = d1_96h_summary, aes(ymin = Relative_cell_proliferation - SD, ymax = Relative_cell_proliferation + SD), 
                width = .2, position = position_dodge(0.9)) + 
  scale_fill_manual(values = c("#666666","#666666")) + 
  scale_color_manual(values = c("black" ,"black" )) + ylim(0,2) +
  format.A + labs(x="Androgens (10 nM)", y="Relative cell proliferation (96 hours)")

print(p3)

```

```{r}
pairwise.t.test(d1_96h$Relative_cell_proliferation, d1_96h$Androgens, p.adjust.method = "BH")
```

# combime the plots
```{r}
OHT_LNCaP_overtime <- plot_grid(p1,p2,p3, labels=c("a", "b", "c"), ncol = 3, nrow = 1)
print(OHT_LNCaP_overtime)
ggsave("C:/UIUC documents/Androgen manuscript/Data and codes/Figures/11OHT_LNCaP_overtime.pdf", height = 3.5, width = 4 , useDingbats = FALSE)

```

#statistics overtime
```{r}

d1_VC <- subset(d1, Androgens =="VC")
pairwise.t.test(d1_VC$Relative_cell_proliferation, d1_VC$Time, p.adjust.method = "BH")

d1_11OHT <- subset(d1, Androgens =="11OHT")
pairwise.t.test(d1_11OHT$Relative_cell_proliferation, d1_11OHT$Time, p.adjust.method = "BH")

```

```{r}
sessionInfo()
```

